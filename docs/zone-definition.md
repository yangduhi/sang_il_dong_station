# Zone definition

Current seed data includes the initial 13-zone operating concept, with a minimum seed subset committed for bootstrap.

Key principle:
- keep `강동·송파` and `강남·서초` separate
- isolate `하남·구리·남양주` as a nearby hinterland zone
- allow fallback buckets when live coverage is incomplete
